# 五子棋核心逻辑编码任务规划（tasks.md）

> 基于 `design.md` 技术设计与已实现的 `game.h`（40 行）/ `game.c`（144 行）编写。原始需求 12 项功能已 100% 实现，本任务清单聚焦：核心逻辑验证、测试体系建设、可选增量优化、审查与验证。

---

## 1. 核心逻辑验证

验证 `game.h`/`game.c` 中已实现的功能符合原始需求与 `design.md` 接口契约，确认存量代码可作为可信基线。

### 1.1 验证初始化与重开

- [ ] 验证 `initGame()` 执行后 `board` 全部为 0、`currentPlayer=1`、`gameOver=0`、`historyCount=0`（对应 `game.c:11-22`）
- [ ] 验证 `restartGame()` 调用后状态等价于 `initGame()`（对应 `game.c:127-130`）
- [ ] 验证头文件保护 `#ifndef GAME_H` 生效，重复包含不会引发重定义（对应 `game.h:1-2,40`）

### 1.2 验证落子流程

- [ ] 验证 `placePiece` 边界校验：`row/col` 越界（负数或 ≥15）返回 0 且不修改任何全局状态（对应 `game.c:28-30`）
- [ ] 验证 `placePiece` 游戏结束校验：`gameOver==1` 时任意坐标返回 0（对应 `game.c:32-34`）
- [ ] 验证 `placePiece` 占用校验：已落子位置返回 0 且不覆盖原棋子（对应 `game.c:36-38`）
- [ ] 验证 `placePiece` 成功落子后 `board[row][col]=currentPlayer`、`historyCount` 自增、历史记录三元组正确（对应 `game.c:41-47`）
- [ ] 验证 `placePiece` 胜利后置 `gameOver=1`、返回 1 且不切换玩家（对应 `game.c:50-53`）
- [ ] 验证 `placePiece` 平局后置 `gameOver=1`、返回 1（对应 `game.c:56-59`）
- [ ] 验证 `placePiece` 正常落子（未胜未平）后切换玩家 1↔2 并返回 1（对应 `game.c:62-64`）

### 1.3 验证胜负判定

- [ ] 验证 `checkWin` 水平方向连5判胜、不足5返回0（对应 `game.c:69-102`，方向 `{0,1}`）
- [ ] 验证 `checkWin` 垂直方向连5判胜（方向 `{1,0}`）
- [ ] 验证 `checkWin` 主对角线方向连5判胜（方向 `{1,1}`）
- [ ] 验证 `checkWin` 副对角线方向连5判胜（方向 `{1,-1}`）
- [ ] 验证 `checkWin` 在棋盘边缘/四角落子时延伸不越界、连子统计正确

### 1.4 验证悔棋与平局

- [ ] 验证 `undoMove` 在 `historyCount<=0` 时静默返回、不修改状态（对应 `game.c:107-109`）
- [ ] 验证 `undoMove` 正常悔棋后 `historyCount` 自减、对应格置空、`currentPlayer` 恢复为该步执棋方、`gameOver=0`（对应 `game.c:111-123`）
- [ ] 验证 `checkDraw` 棋盘存在空位时返回 0、棋盘全满时返回 1（对应 `game.c:133-144`）
- [ ] 验证全局状态一致性：`historyCount` 始终等于 `history` 有效记录数与 `board` 非空格数

## 2. 测试体系建设

建立可重复运行的测试体系，覆盖核心逻辑、边界条件与完整对局流程。

### 2.1 搭建测试框架

- [ ] 创建测试驱动文件 `test_game.c`，包含 `main` 入口与断言辅助宏（如 `ASSERT_TRUE`/`ASSERT_EQ`），`#include "game.h"` 并链接 `game.c`
- [ ] 提供编译构建方式（Makefile 或 `build.bat` 脚本），支持一键编译运行 `game.c` + `test_game.c` 并输出通过/失败汇总

### 2.2 编写单元测试

- [ ] 为 `initGame` 编写单元测试，断言四个全局状态全部正确初始化
- [ ] 为 `placePiece` 编写单元测试，覆盖越界/已占用/已结束/成功四种返回路径及副作用
- [ ] 为 `checkWin` 编写单元测试，覆盖四方向连5、不足5、边缘落子场景
- [ ] 为 `undoMove` 编写单元测试，覆盖空历史与正常悔棋后状态恢复
- [ ] 为 `checkDraw` 编写单元测试，覆盖有空位、恰好全满两种场景

### 2.3 编写集成与端到端测试

- [ ] 编写黑方连5胜局集成测试：按序落子形成五连，验证 `gameOver=1` 且玩家未切换
- [ ] 编写白方连5胜局集成测试：验证白方获胜路径与玩家切换顺序正确
- [ ] 编写平局集成测试：模拟填满棋盘且无连5，验证 `checkDraw` 返回 1 且 `placePiece` 置 `gameOver=1`
- [ ] 编写悔棋后继续对局端到端测试：落子→悔棋→重新落子，验证状态一致性且可继续判胜
- [ ] 编写边界条件测试：在棋盘四角与四边落子并构造连5，验证判定正确不越界

## 3. 可选增量优化

基于 `design.md` 第 2.3.4 节演进路线与第 1.1.2 节增强建议，按风险与价值排序，按需选用，非强制。

### 3.1 历史步数上限防御性校验（推荐，低风险）

- [ ] 在 `placePiece`（`game.c:43-47`）记录历史前增加 `historyCount < MAX_HISTORY` 显式校验，越界时返回 0，增强防御性并消除潜在数组越界隐患

### 3.2 落子错误码细化（可选）

- [ ] 在 `game.h` 新增枚举 `PLACE_OK/OUT_OF_BOUNDS/CELL_OCCUPIED/GAME_OVER`，`placePiece` 返回细分错误码，便于上层 UI 精确提示（需评估对现有 0/1 返回契约的向后兼容性）

### 3.3 长连禁手规则（可选）

- [ ] 将 `checkWin` 中 `count >= 5` 改为 `count == 5` 以适配标准竞赛规则，并补充长连禁手判定逻辑（当前实现为"无禁手自由五子棋"）

### 3.4 多实例支持（可选，较大改动）

- [ ] 将全局状态封装为 `typedef struct { ... } Game;`，控制层与判定层函数增加 `Game*` 入参，支持多局并发对弈

### 3.5 持久化与复盘接口（可选）

- [ ] 新增 `saveGame/loadGame` 接口，将 `board/history/historyCount/currentPlayer` 序列化到文件，支持存档读档
- [ ] 基于 `history` 新增复盘回放接口，支持逐步重现对局过程

## 4. 审查与验证

确保交付质量、设计与实现一致性及变更可控。

### 4.1 代码审查

- [ ] 审查 `game.h`：头文件保护、`extern` 声明、宏定义、函数原型完整且与实现一致
- [ ] 审查 `game.c`：各函数边界检查完备、无数组越界访问、全局状态修改路径清晰
- [ ] 审查命名规范：全局变量与函数统一 camelCase，与需求及用户偏好一致

### 4.2 设计一致性核对

- [ ] 核对 `design.md` 第 2.2 节接口契约与 `game.h`/`game.c` 实现签名、前置/后置条件逐一对应
- [ ] 核对已实现功能与原始需求 12 项功能（`SIZE`/`board`/`currentPlayer`/`gameOver`/`history`/`historyCount`/`initGame`/`placePiece`/`checkWin`/`undoMove`/`restartGame`/`checkDraw`）一一覆盖

### 4.3 变更范围与测试确认

- [ ] 确认增量优化（任务组 3）改动范围、是否启用及对现有契约的向后兼容性
- [ ] 确认测试体系（任务组 2）全部用例通过，核心逻辑验证（任务组 1）无缺陷遗留
- [ ] 记录最终交付清单：基线代码状态、已启用增量、测试结果汇总